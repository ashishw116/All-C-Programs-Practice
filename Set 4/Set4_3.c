//Write a program to take population of a country in crores with its growth rate per year and calculate the population for the end of the year.( Hint use Specifier : %ld)
#include<stdio.h>
void main()
{
    long int population;
    printf("Enter The Population=");
    scanf("%ld",&population);

}

//WAP to illustrate the use of Size of operator.
#include <stdio.h>
void main()
{
    int a;
    long int b;
    float c;
    char ch;
    printf("\n Size of For int a variable=%d",sizeof(a));
    printf("\n Size of For long int b variable=%d",sizeof(b));
    printf("\n Size of For float c variable=%d",sizeof(c));
    printf("\n Size of For char ch variable=%d",sizeof(ch));
}
